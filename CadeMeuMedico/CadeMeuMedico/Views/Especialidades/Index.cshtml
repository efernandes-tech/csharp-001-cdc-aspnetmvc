@model IEnumerable<CadeMeuMedico.Models.Especialidades>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Adicionar nova especialidade", "Adicionar")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.ActionLink("Editar", "Editar", new { id=item.IDEspecialidade }) |

            @Html.ActionLink("Details", "Details", new { id=item.IDEspecialidade }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDEspecialidade })
        </td>
    </tr>
}

</table>
